for(int i = 0; i < str.length(); i++) {
    char c = str.charAt(i);
    if(Character.isUpperCase(c))
        System.out.print(Character.toLowerCase(c));
    else
        System.out.print(Character.toUpperCase(c));
}

Now:
TC: O(n)

SC: O(1)

📌 Interview line:

“By using charAt() instead of toCharArray(), we avoid extra space.”
