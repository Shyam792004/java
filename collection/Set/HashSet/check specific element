
        Set<Integer>st=new HashSet<>();
        
        int n=sc.nextInt();
        for(int i=0;i<n;i++)
        {
          st.add(sc.nextInt());
        }
        
        int tar=sc.nextInt();
        
        if(st.contains(tar))
        {
        System.out.println("found");
        }
